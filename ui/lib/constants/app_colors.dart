import 'package:flutter/material.dart';

class AppColors {
  static const Color seed = Colors.teal;
  static const Color connect = Colors.green;
  static const Color connecting = Colors.amber;
  static const Color disconnect = Colors.red;
}
